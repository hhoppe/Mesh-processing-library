<?xml version="1.0" encoding="utf-8"?>
<Project ToolsVersion="4.0" xmlns="http://schemas.microsoft.com/developer/msbuild/2003">
  <!-- This file is intended for use after the import of $(VCTargetsPath)\Microsoft.Cpp.Default.props and
       before the import of $(VCTargetsPath)\Microsoft.Cpp.props. -->

  <!-- Explicitly select the platform toolset, to avoid the "Retarget solution" nag from VS. -->
  <PropertyGroup>
    <PlatformToolset>$(DefaultPlatformToolset)</PlatformToolset>
  </PropertyGroup>
</Project>

<!-- For Emacs:
     Local Variables:
     nxml-child-indent: 2
     End:
-->
