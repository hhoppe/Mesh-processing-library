# For Microsoft cl.exe compiler, using MSVC STL as C++ library, vcruntime + Microsoft's Universal CRT (ucrt),
# Win32 threads, and Win32 API.

release ?= 0#  0=debug or 1=release (usually 0).

include $(dir $(lastword $(MAKEFILE_LIST)))Makefile_base_win
